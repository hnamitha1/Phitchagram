<h1>Phitchagram</h1>
<% @posts.each do |post| %>
  <%= link_to (image_tag post.image.url(:medium)), post_path(post) %>
    <p>
      <%= post.caption %>
    </p>
<% end %>
<%= link_to "New Post", new_post_path %>